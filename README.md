# E-commerce проект

## Описание

Проект представляет собой ядро для интернет-магазина. Реализованы основные сущности:

- **Product** — товар (название, описание, цена, количество)
- **Category** — категория товаров (название, описание, список товаров)

## Установка

```
git clone https://github.com/sergejgrigorev2861-sys/project_e_commerce.git
cd project_e_commerce
poetry install
```
## Ссылка на репозиторий
https://github.com/sergejgrigorev2861-sys/project_e_commerce

## Покрытие тестами

100% (6 тестов)

## Отчёт о покрытии

```
poetry run pytest --cov=src --cov-report=html
```
## Домашнее задание 14.2

- Добавлены приватные атрибуты `_products` в `Category` и `_price` в `Product`
- Реализован метод `add_product` для добавления продуктов в категорию
- Добавлен геттер `products` (возвращает строку с продуктами)
- Добавлен класс-метод `new_product` (создание продукта из словаря)
- Добавлены геттер и сеттер для `price` с проверкой на положительное значение
- Написаны тесты, покрытие — 100%

## Дополнительные задания (14.2)

- Класс-метод `new_product` теперь проверяет дубликаты по имени:
- если товар уже есть — количество суммируется, цена выбирается максимальная
- Сеттер `price` запрашивает подтверждение при понижении цены (y/n)
- Написаны тесты для новой функциональности, покрытие — 100%

## Домашнее задание 15.1

Добавлены магические методы:
- `__str__` для `Product` и `Category`
- `__add__` для `Product` (сумма стоимости товаров на складе)
- Итератор `CategoryIterator` для перебора продуктов в категории

## Домашнее задание 16.1 — Наследование и ограничения

### Классы-наследники

Добавлены два класса, наследующих от `Product`:

- **`Smartphone`** — смартфон с дополнительными атрибутами:
  - `efficiency` — производительность
  - `model` — модель
  - `memory` — объём встроенной памяти
  - `color` — цвет

- **`LawnGrass`** — газонная трава с дополнительными атрибутами:
  - `country` — страна-производитель
  - `germination_period` — срок прорастания
  - `color` — цвет

### Ограничение сложения

В методе `__add__` добавлена проверка: складывать можно только товары одного класса.  
При попытке сложить объекты разных классов выбрасывается `TypeError`.

### Ограничение добавления

Метод `add_product` в классе `Category` теперь проверяет, что добавляемый объект является экземпляром `Product` или его наследником.  
В противном случае выбрасывается `TypeError`.

### Тестирование

- Добавлены тесты для новых классов и проверок
- Покрытие: **100%**
- Количество тестов: **29**


## Домашнее задание 16.2 — Абстрактные классы и миксины

### Абстрактный класс `BaseProduct`
- Создан абстрактный класс `BaseProduct` (наследник `ABC`)
- Определён абстрактный метод `get_info()`, который возвращает информацию о продукте
- Класс `Product` унаследован от `BaseProduct`, что гарантирует наличие метода `get_info()` у всех продуктов

### Миксин `ReprMixin`
- Реализован класс-миксин `ReprMixin`, который добавляет метод `__repr__`
- При создании объекта выводится информация о классе и переданных параметрах
- Миксин добавлен в цепочку наследования `Product` (множественное наследование)

### Абстрактный класс `BaseContainer`
- Создан абстрактный класс `BaseContainer` для общих свойств контейнеров
- Определены абстрактные методы `get_items()` и `get_total_quantity()`
- Классы `Category` и `Order` унаследованы от `BaseContainer`

### Класс `Order`
- Добавлен класс `Order` для оформления заказа на один товар
- Хранит ссылку на товар, количество и итоговую стоимость
- Реализует методы `get_items()` и `get_total_quantity()`

### Тестирование
- Добавлены тесты для абстрактного класса и миксина
- Все тесты: **40**, покрытие: **100%**

### Запуск

```bash
poetry install
poetry run pytest
poetry run python main.py
text
```

## Лицензия

Проект распространяется под лицензией MIT.